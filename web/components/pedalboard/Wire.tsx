/**
 * The dashed signal wire between rail cards (reference `.wire`) — flexes to
 * fill the gap; `pb-wireflow` scrolls the dashes to show signal direction.
 */
export function Wire() {
  return <div className="pb-wire" aria-hidden="true" />;
}
